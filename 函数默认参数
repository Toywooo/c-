#include<iostream>
using namespace std;
#include<string>
int func(int a,int b,int c=10)//如果某个位置有了默认参数，那么从这个位置往后，从左到右都必须有默认值
{
	return a+b+c;
}
int main()
{
	int a=5,b=10,c=80;
	int ret=func(a,b,c);
	cout<<"ret="<<ret<<endl;
	system("pause");
	return 0;
}
